name: star watcher

on:
  watch:
    types: started

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: |
          TZ=Europe/Berlin date "+%y-%m-%d %H:%M:%S" | xargs -I {} echo "[{}] 被点star了"
